when:
  - event: [push, pull_request, manual]

labels:
  platform: linux/amd64

steps:
  - name: lint
    image: golang:1.24
    commands:
      - go vet ./...

  - name: test
    image: golang:1.24
    commands:
      - go test -v -race -count=1 ./...
    depends_on: [lint]
